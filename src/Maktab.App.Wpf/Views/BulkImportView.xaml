<UserControl x:Class="Maktab.App.Wpf.Views.BulkImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             FlowDirection="RightToLeft">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#334155" Padding="14" CornerRadius="6" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="📥 ورود اطلاعات دسته‌جمعی" FontSize="20" FontWeight="Bold" Foreground="White" />
                <TextBlock Text=" (Bulk Import)" FontSize="13" Foreground="#CBD5E1" VerticalAlignment="Center" Margin="10,2,0,0" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="14" />
                <ColumnDefinition Width="300" />
            </Grid.ColumnDefinitions>

            <!-- CSV Paste / File Select -->
            <Border Grid.Column="0" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <Button Content="📁 انتخاب فایل CSV" Height="32" Padding="12,0" Background="#3B82F6" Foreground="White" FontWeight="SemiBold" Click="BrowseFileButton_Click" Margin="0,0,8,0" />
                        <Button Content="📥 اجرای ورود" Height="32" Padding="12,0" Background="#16A34A" Foreground="White" FontWeight="Bold" Click="ImportButton_Click" Margin="0,0,8,0" />
                        <TextBlock x:Name="FilePathTextBlock" VerticalAlignment="Center" Foreground="#475569" />
                    </StackPanel>
                    <TextBox x:Name="CsvTextBox" Grid.Row="1" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                             FontFamily="Consolas" FontSize="12" Padding="8" Background="#F8FAFC" BorderBrush="#CBD5E1" />
                </Grid>
            </Border>

            <!-- Result Panel -->
            <Border Grid.Column="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="12">
                <StackPanel>
                    <TextBlock Text="نتایج" FontWeight="Bold" FontSize="15" Foreground="#334155" Margin="0,0,0,12" />
                    <TextBlock x:Name="SummaryTextBlock" Text="۰ ردیف وارد شد" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <TextBox x:Name="ErrorsTextBox" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto" Height="200" FontSize="11" Background="#FEF2F2" Foreground="#DC2626" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
