<UserControl x:Class="Maktab.App.Wpf.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             FlowDirection="RightToLeft">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#334155" Padding="14" CornerRadius="6" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="👥 مدیریت کاربران" FontSize="20" FontWeight="Bold" Foreground="White" />
                <TextBlock Text=" (User Management)" FontSize="13" Foreground="#CBD5E1" VerticalAlignment="Center" Margin="10,2,0,0" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="14" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- User Form -->
            <Border Grid.Column="0" Background="#FAFAFA" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="14">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="مشخصات کاربر" FontSize="16" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,12" />

                        <TextBlock Text="نام کاربری (Username) *" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBox x:Name="UsernameTextBox" Height="32" Margin="0,0,0,10" MaxLength="50" />

                        <TextBlock Text="رمز عبور (Password) *" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <PasswordBox x:Name="PasswordBox" Height="32" Margin="0,0,0,10" MaxLength="50" />

                        <TextBlock Text="نام کامل (Full Name) *" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBox x:Name="FullNameTextBox" Height="32" Margin="0,0,0,10" MaxLength="100" />

                        <TextBlock Text="نقش (Role) *" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <ComboBox x:Name="RoleComboBox" Height="32" Margin="0,0,0,10" />

                        <CheckBox x:Name="IsActiveCheckBox" Content="فعال (Active)" Margin="0,0,0,16" IsChecked="True" />

                        <UniformGrid Columns="2" Rows="2">
                            <Button Margin="0,0,4,8" Height="36" Content="➕ افزودن" Background="#16A34A" Foreground="White" FontWeight="Bold" Click="AddUserButton_Click" />
                            <Button Margin="4,0,0,8" Height="36" Content="✏️ ویرایش" Background="#D97706" Foreground="White" FontWeight="Bold" Click="UpdateUserButton_Click" />
                            <Button Margin="0,4,4,0" Height="36" Content="🗑️ حذف" Background="#DC2626" Foreground="White" FontWeight="Bold" Click="DeleteUserButton_Click" />
                            <Button Margin="4,4,0,0" Height="36" Content="🧹 پاک کردن" Background="#64748B" Foreground="White" FontWeight="SemiBold" Click="ClearFormButton_Click" />
                        </UniformGrid>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Users DataGrid -->
            <Border Grid.Column="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="لیست کاربران" FontWeight="Bold" FontSize="15" Foreground="#334155" Margin="4,4,4,8" />
                    <DataGrid x:Name="UsersDataGrid"
                              Grid.Row="1"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="#E2E8F0"
                              SelectionChanged="UsersDataGrid_SelectionChanged"
                              RowHeight="32">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="آیدی" Binding="{Binding UserId}" Width="60" />
                            <DataGridTextColumn Header="نام کاربری" Binding="{Binding Username}" Width="*" />
                            <DataGridTextColumn Header="نام کامل" Binding="{Binding FullName}" Width="*" />
                            <DataGridTextColumn Header="نقش" Binding="{Binding Role}" Width="100" />
                            <DataGridTextColumn Header="وضعیت" Binding="{Binding IsActive}" Width="70" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
