<UserControl x:Class="Maktab.App.Wpf.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             FlowDirection="RightToLeft">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#4C1D95" Padding="14" CornerRadius="6" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="📚 مدیریت کتابخانه" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" />
                <TextBlock Text=" (Library — Books &amp; Loans)" FontSize="13" Foreground="#C4B5FD" VerticalAlignment="Center" Margin="10,2,0,0" />
            </StackPanel>
        </Border>

        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
            <!-- Tab 1: Books Inventory -->
            <TabItem Header="📖 ثبت و موجودی کتب">
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="320" />
                        <ColumnDefinition Width="14" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Book Form -->
                    <Border Grid.Column="0" Background="#FAFAFA" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="14">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Text="مشخصات کتاب" FontSize="16" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,12" />

                                <TextBlock Text="عنوان کتاب *" FontWeight="SemiBold" Margin="0,0,0,4" />
                                <TextBox x:Name="BookTitleTextBox" Height="32" VerticalContentAlignment="Center" Margin="0,0,0,10" MaxLength="100" />

                                <TextBlock Text="مولف (Author)" FontWeight="SemiBold" Margin="0,0,0,4" />
                                <TextBox x:Name="BookAuthorTextBox" Height="32" VerticalContentAlignment="Center" Margin="0,0,0,10" MaxLength="100" />

                                <TextBlock Text="کتگوری (Category)" FontWeight="SemiBold" Margin="0,0,0,4" />
                                <TextBox x:Name="BookCategoryTextBox" Height="32" VerticalContentAlignment="Center" Margin="0,0,0,10" MaxLength="50" />

                                <TextBlock Text="تعداد نسخه‌ها *" FontWeight="SemiBold" Margin="0,0,0,4" />
                                <TextBox x:Name="BookCopiesTextBox" Height="32" VerticalContentAlignment="Center" Margin="0,0,0,16" />

                                <UniformGrid Columns="2" Rows="2" Margin="0,0,0,10">
                                    <Button Margin="0,0,4,8" Height="36" Content="➕ ثبت کتاب" Background="#16A34A" Foreground="White" FontWeight="Bold" Click="AddBookButton_Click" />
                                    <Button Margin="4,0,0,8" Height="36" Content="✏️ ویرایش" Background="#D97706" Foreground="White" FontWeight="Bold" Click="UpdateBookButton_Click" />
                                    <Button Margin="0,4,4,0" Height="36" Content="🗑️ حذف" Background="#DC2626" Foreground="White" FontWeight="Bold" Click="DeleteBookButton_Click" />
                                    <Button Margin="4,4,0,0" Height="36" Content="🧹 پاک کردن" Background="#64748B" Foreground="White" FontWeight="SemiBold" Click="ClearBookFormButton_Click" />
                                </UniformGrid>

                                <TextBlock x:Name="BookFormStatusTextBlock" Text="" Foreground="#059669" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,8,0,0" />
                            </StackPanel>
                        </ScrollViewer>
                    </Border>

                    <!-- Books DataGrid -->
                    <Border Grid.Column="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="8">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <StackPanel Orientation="Horizontal" Margin="4,4,4,8">
                                <TextBlock Text="لیست کتب کتابخانه" FontWeight="Bold" FontSize="15" Foreground="#334155" />
                                <TextBlock x:Name="BooksCountTextBlock" Text=" (تعداد: 0)" Foreground="#64748B" Margin="6,2,0,0" />
                            </StackPanel>
                            <DataGrid x:Name="BooksDataGrid"
                                      Grid.Row="1"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      IsReadOnly="True"
                                      SelectionMode="Single"
                                      GridLinesVisibility="Horizontal"
                                      HorizontalGridLinesBrush="#E2E8F0"
                                      SelectionChanged="BooksDataGrid_SelectionChanged"
                                      RowHeight="32">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="آیدی" Binding="{Binding BookId}" Width="60" />
                                    <DataGridTextColumn Header="عنوان" Binding="{Binding Title}" Width="*" />
                                    <DataGridTextColumn Header="مولف" Binding="{Binding Author}" Width="*" />
                                    <DataGridTextColumn Header="کتگوری" Binding="{Binding Category}" Width="110" />
                                    <DataGridTextColumn Header="تعداد کل" Binding="{Binding TotalCopies}" Width="80" />
                                    <DataGridTextColumn Header="موجود" Binding="{Binding AvailableCopies}" Width="80" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Tab 2: Loans -->
            <TabItem Header="🔄 امانت و بازگشت کتب">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- Issue Form -->
                    <Border Grid.Row="0" Background="#F5F3FF" BorderBrush="#DDD6FE" BorderThickness="1" Padding="10" CornerRadius="6" Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="220" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="130" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="180" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="70" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="کتاب:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,6,0" />
                            <ComboBox x:Name="LoanBookComboBox" Grid.Column="1" Height="32" Margin="0,0,10,0" DisplayMemberPath="Title" SelectedValuePath="BookId" />

                            <TextBlock Grid.Column="2" Text="صنف:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,6,0" />
                            <ComboBox x:Name="LoanClassComboBox" Grid.Column="3" Height="32" Margin="0,0,10,0" DisplayMemberPath="GradeName" SelectedValuePath="ClassId" SelectionChanged="LoanClassComboBox_SelectionChanged" />

                            <TextBlock Grid.Column="4" Text="شاگرد:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,6,0" />
                            <ComboBox x:Name="LoanStudentComboBox" Grid.Column="5" Height="32" Margin="0,0,10,0" DisplayMemberPath="DisplayName" SelectedValuePath="StudentId" />

                            <TextBlock Grid.Column="6" Text="مدت (روز):" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,6,0" />
                            <TextBox x:Name="LoanDaysTextBox" Grid.Column="7" Height="32" VerticalContentAlignment="Center" Text="14" Margin="0,0,10,0" />

                            <Button Grid.Column="8" Content="📤 امانت کتاب" Height="32" Padding="14,0" Background="#7C3AED" Foreground="White" FontWeight="Bold" Click="IssueBookButton_Click" />
                        </Grid>
                    </Border>

                    <!-- Loans DataGrid -->
                    <Border Grid.Row="1" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="8">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <WrapPanel Margin="4,4,4,8">
                                <TextBlock Text="لیست امانات" FontWeight="Bold" FontSize="15" Foreground="#334155" VerticalAlignment="Center" />
                                <CheckBox x:Name="ActiveLoansOnlyCheckBox" Content="فقط امانات فعال" Margin="14,0,0,0" IsChecked="True" VerticalAlignment="Center" Checked="LoanFilter_Changed" Unchecked="LoanFilter_Changed" />
                                <CheckBox x:Name="OverdueOnlyCheckBox" Content="فقط معوقات" Margin="14,0,0,0" VerticalAlignment="Center" Checked="LoanFilter_Changed" Unchecked="LoanFilter_Changed" />
                                <Button Content="🔄 تازه‌سازی" Height="28" Padding="10,0" Margin="14,0,0,0" Background="#3B82F6" Foreground="White" Click="RefreshLoansButton_Click" />
                            </WrapPanel>
                            <DataGrid x:Name="LoansDataGrid"
                                      Grid.Row="1"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      IsReadOnly="True"
                                      SelectionMode="Single"
                                      GridLinesVisibility="Horizontal"
                                      HorizontalGridLinesBrush="#E2E8F0"
                                      RowHeight="32">
                                <DataGrid.RowStyle>
                                    <Style TargetType="DataGridRow">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsOverdue}" Value="True">
                                                <Setter Property="Background" Value="#FEE2E2" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGrid.RowStyle>
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="آیدی" Binding="{Binding LoanId}" Width="55" />
                                    <DataGridTextColumn Header="کتاب" Binding="{Binding BookTitle}" Width="*" />
                                    <DataGridTextColumn Header="شاگرد" Binding="{Binding StudentName}" Width="*" />
                                    <DataGridTextColumn Header="شماره اساس" Binding="{Binding RollNumber}" Width="85" />
                                    <DataGridTextColumn Header="تاریخ امانت" Binding="{Binding IssueDate}" Width="95" />
                                    <DataGridTextColumn Header="تاریخ واپسی" Binding="{Binding DueDate}" Width="95" />
                                    <DataGridTextColumn Header="بازگشت" Binding="{Binding ReturnDateText}" Width="95" />
                                    <DataGridTextColumn Header="وضعیت" Binding="{Binding StatusText}" Width="110" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="4,8,4,0">
                        <Button Content="📥 ثبت بازگشت کتاب انتخاب‌شده" Height="34" Padding="14,0" Background="#16A34A" Foreground="White" FontWeight="Bold" Click="ReturnBookButton_Click" />
                        <TextBlock x:Name="LoanStatusTextBlock" Text="" FontSize="12" Foreground="#059669" FontWeight="SemiBold" VerticalAlignment="Center" Margin="12,0,0,0" />
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
