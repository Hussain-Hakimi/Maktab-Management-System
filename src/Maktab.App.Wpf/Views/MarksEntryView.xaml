<UserControl x:Class="Maktab.App.Wpf.Views.MarksEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1000"
             FlowDirection="RightToLeft">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#0F766E" Padding="14" CornerRadius="6" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📝 جدول ثبت و ویرایش نمرات امتحانات" FontSize="20" FontWeight="Bold" Foreground="White" />
                    <TextBlock Text=" (Exam Marks Entry &amp; Auto-Grading)" FontSize="13" Foreground="#99F6E4" VerticalAlignment="Center" Margin="10,2,0,0" />
                </StackPanel>
                <Border Grid.Column="1" Background="#115E59" CornerRadius="4" Padding="8,4">
                    <TextBlock Text="قانون: چهارونیم‌ماهه (۰-۴۰) + سالانه (۰-۶۰) = مجموع ۱۰۰ (قبولی: ۴۰)" FontSize="12" Foreground="White" />
                </Border>
            </Grid>
        </Border>

        <!-- Selection Panel -->
        <Border Grid.Row="1" Background="#F0FDFA" BorderBrush="#99F6E4" BorderThickness="1" Padding="10" CornerRadius="6" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="180" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="انتخاب صنف:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,8,0" />
                <ComboBox x:Name="ClassComboBox" Grid.Column="1" Height="32" DisplayMemberPath="GradeName" SelectedValuePath="ClassId" SelectionChanged="ClassComboBox_SelectionChanged" />

                <TextBlock Grid.Column="3" Text="انتخاب مضمون:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,8,0" />
                <ComboBox x:Name="SubjectComboBox" Grid.Column="4" Height="32" DisplayMemberPath="SubjectName" SelectedValuePath="SubjectId" SelectionChanged="SubjectComboBox_SelectionChanged" />

                <StackPanel Grid.Column="6" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="StudentCountTextBlock" Text="شاگردان: ۰ نفر" FontWeight="SemiBold" Foreground="#0F766E" Margin="10,0,0,0" />
                </StackPanel>

                <Button Grid.Column="7" Content="🔄 بارگذاری مجدد" Height="32" Padding="12,0" Background="#0D9488" Foreground="White" FontWeight="SemiBold" Click="ReloadButton_Click" />
            </Grid>
        </Border>

        <!-- Marks DataGrid -->
        <Border Grid.Row="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <DataGrid x:Name="MarksDataGrid"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          SelectionMode="Single"
                          GridLinesVisibility="All"
                          HorizontalGridLinesBrush="#E2E8F0"
                          VerticalGridLinesBrush="#E2E8F0"
                          RowHeight="34"
                          CellEditEnding="MarksDataGrid_CellEditEnding">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="شماره اساس" Binding="{Binding RollNumber}" IsReadOnly="True" Width="90" />
                        <DataGridTextColumn Header="نام" Binding="{Binding FirstName}" IsReadOnly="True" Width="120" />
                        <DataGridTextColumn Header="تخلص" Binding="{Binding LastName}" IsReadOnly="True" Width="120" />
                        <DataGridTextColumn Header="نام پدر" Binding="{Binding FatherName}" IsReadOnly="True" Width="120" />
                        <DataGridTextColumn Header="چهارونیم‌ماهه (۰-۴۰) ✏️" Binding="{Binding MidtermScore, StringFormat={}{0:0.##}, UpdateSourceTrigger=PropertyChanged}" Width="150" />
                        <DataGridTextColumn Header="سالانه (۰-۶۰) ✏️" Binding="{Binding FinalScore, StringFormat={}{0:0.##}, UpdateSourceTrigger=PropertyChanged}" Width="140" />
                        <DataGridTextColumn Header="مجموع (۱۰۰)" Binding="{Binding TotalScore, StringFormat={}{0:0.##}}" IsReadOnly="True" FontWeight="Bold" Width="100" />
                        <DataGridTextColumn Header="فیصد" Binding="{Binding Percentage, StringFormat={}{0:0.##}%}" IsReadOnly="True" Width="80" />
                        <DataGridTextColumn Header="درجه" Binding="{Binding Grade}" IsReadOnly="True" FontWeight="Bold" Width="70" />
                        <DataGridTextColumn Header="نتیجه" Binding="{Binding ResultText}" IsReadOnly="True" FontWeight="Bold" Width="90" />
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock Grid.Row="1" Margin="4,8,4,4" Text="💡 برای درج یا تغییر نمرات، روی ستون‌های نمره کلیک کرده و عدد را وارد نمایید. مجموع و نتیجه به صورت خودکار محاسبه می‌گردد." FontSize="12" Foreground="#64748B" />
            </Grid>
        </Border>

        <!-- Bottom Action Bar -->
        <Border Grid.Row="3" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" Padding="10" CornerRadius="6" Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="SaveStatusTextBlock" VerticalAlignment="Center" FontWeight="Bold" FontSize="14" Foreground="#16A34A" Text="" />

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="💾 ثبت و ذخیره نمرات در دیتابیس" Height="38" Padding="20,0" Background="#16A34A" Foreground="White" FontWeight="Bold" FontSize="14" Click="SaveMarksButton_Click" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
