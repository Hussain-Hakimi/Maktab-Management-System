<UserControl x:Class="Maktab.App.Wpf.Views.AttendanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Maktab.App.Wpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1000"
             FlowDirection="RightToLeft">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#0F766E" Padding="14" CornerRadius="6" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🗓️ ثبت حاضری روزانه شاگردان" FontSize="20" FontWeight="Bold" Foreground="White" />
                    <TextBlock Text=" (Attendance Entry)" FontSize="13" Foreground="#99F6E4" VerticalAlignment="Center" Margin="10,2,0,0" />
                </StackPanel>
                <Border Grid.Column="1" Background="#115E59" CornerRadius="4" Padding="8,4">
                    <TextBlock Text="قانون: بیش از ۳۰ روز غیرحاضری باعث تکرار صنف می‌شود" FontSize="12" Foreground="White" />
                </Border>
            </Grid>
        </Border>

        <!-- Selection Panel -->
        <Border Grid.Row="1" Background="#F0FDFA" BorderBrush="#99F6E4" BorderThickness="1" Padding="10" CornerRadius="6" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="180" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="انتخاب صنف:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,8,0" />
                <ComboBox x:Name="ClassComboBox" Grid.Column="1" Height="32" DisplayMemberPath="GradeName" SelectedValuePath="ClassId" SelectionChanged="ClassComboBox_SelectionChanged" />

                <TextBlock Grid.Column="3" Text="تاریخ:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,8,0" />
                <DatePicker x:Name="AttendanceDatePicker" Grid.Column="4" Height="32" SelectedDateChanged="AttendanceDatePicker_SelectedDateChanged" />

                <TextBlock Grid.Column="6" Text="تاریخ هجری شمسی:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,8,0" />
                <TextBlock x:Name="ShamsiDateTextBlock" Grid.Column="7" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#0F766E" />

                <Button Grid.Column="8" Content="🔄 بارگذاری مجدد" Height="32" Padding="12,0" Background="#0D9488" Foreground="White" FontWeight="SemiBold" Click="ReloadButton_Click" />
            </Grid>
        </Border>

        <!-- Attendance DataGrid -->
        <Border Grid.Row="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <DataGrid x:Name="AttendanceDataGrid"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          SelectionMode="Single"
                          GridLinesVisibility="All"
                          HorizontalGridLinesBrush="#E2E8F0"
                          VerticalGridLinesBrush="#E2E8F0"
                          RowHeight="34">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="شماره اساس" Binding="{Binding RollNumber}" IsReadOnly="True" Width="90" />
                        <DataGridTextColumn Header="نام" Binding="{Binding FirstName}" IsReadOnly="True" Width="120" />
                        <DataGridTextColumn Header="تخلص" Binding="{Binding LastName}" IsReadOnly="True" Width="120" />
                        <DataGridTextColumn Header="نام پدر" Binding="{Binding FatherName}" IsReadOnly="True" Width="120" />
                        <DataGridComboBoxColumn Header="وضعیت حاضری" SelectedItemBinding="{Binding Status}" Width="150">
                            <DataGridComboBoxColumn.ElementStyle>
                                <Style TargetType="ComboBox">
                                    <Setter Property="ItemsSource" Value="{Binding Source={x:Static local:AttendanceView.Statuses}}" />
                                </Style>
                            </DataGridComboBoxColumn.ElementStyle>
                            <DataGridComboBoxColumn.EditingElementStyle>
                                <Style TargetType="ComboBox">
                                    <Setter Property="ItemsSource" Value="{Binding Source={x:Static local:AttendanceView.Statuses}}" />
                                </Style>
                            </DataGridComboBoxColumn.EditingElementStyle>
                        </DataGridComboBoxColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock Grid.Row="1" Margin="4,8,4,4" Text="💡 برای تغییر وضعیت، روی سلول مربوطه کلیک کنید و یکی از گزینه‌ها را انتخاب نمایید." FontSize="12" Foreground="#64748B" />
            </Grid>
        </Border>

        <!-- Bottom Action Bar -->
        <Border Grid.Row="3" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" Padding="10" CornerRadius="6" Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="SaveStatusTextBlock" VerticalAlignment="Center" FontWeight="Bold" FontSize="14" Foreground="#16A34A" Text="" />

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="💾 ثبت حاضری در دیتابیس" Height="38" Padding="20,0" Background="#16A34A" Foreground="White" FontWeight="Bold" FontSize="14" Click="SaveAttendanceButton_Click" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
