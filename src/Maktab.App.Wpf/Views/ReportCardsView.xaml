<UserControl x:Class="Maktab.App.Wpf.Views.ReportCardsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1000"
             FlowDirection="RightToLeft">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#4338CA" Padding="14" CornerRadius="6" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📄 صدور اطلاع‌نامه و کارنامه نمرات (PDF)" FontSize="20" FontWeight="Bold" Foreground="White" />
                    <TextBlock Text=" (Report Cards Generator - Ittila'a Nama)" FontSize="13" Foreground="#C7D2FE" VerticalAlignment="Center" Margin="10,2,0,0" />
                </StackPanel>
                <Button Grid.Column="1" Content="📂 باز کردن پوشه کارنامه‌ها" Background="#3730A3" Foreground="White" FontWeight="SemiBold" Padding="12,6" Click="OpenReportsFolderButton_Click" />
            </Grid>
        </Border>

        <!-- Controls / Filter Header -->
        <Border Grid.Row="1" Background="#EEF2FF" BorderBrush="#C7D2FE" BorderThickness="1" Padding="12" CornerRadius="6" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="180" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="140" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="140" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="انتخاب صنف:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,6,0" />
                <ComboBox x:Name="ClassComboBox" Grid.Column="1" Height="32" DisplayMemberPath="GradeName" SelectedValuePath="ClassId" SelectionChanged="ClassComboBox_SelectionChanged" />

                <TextBlock Grid.Column="3" Text="انتخاب شاگرد:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,6,0" />
                <ComboBox x:Name="StudentComboBox" Grid.Column="4" Height="32" DisplayMemberPath="DisplayName" SelectedValuePath="StudentId" SelectionChanged="StudentComboBox_SelectionChanged" />

                <TextBlock Grid.Column="6" Text="سال تعلیمی:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,6,0" />
                <TextBox x:Name="AcademicYearTextBox" Grid.Column="7" Height="32" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" />

                <TextBlock Grid.Column="9" Text="نوع اطلاع‌نامه:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,6,0" />
                <ComboBox x:Name="ReportTypeComboBox" Grid.Column="10" Height="32" SelectionChanged="ReportTypeComboBox_SelectionChanged">
                    <ComboBoxItem Content="سالانه (Annual)" Tag="Annual" IsSelected="True" />
                    <ComboBoxItem Content="چهارماهه (Midterm)" Tag="Midterm" />
                </ComboBox>

                <Button Grid.Column="12" Content="🔄 تازه‌سازی" Height="32" Padding="12,0" Background="#6366F1" Foreground="White" FontWeight="SemiBold" Click="RefreshButton_Click" />
            </Grid>
        </Border>

        <!-- Main Content Area: Student Summary & Marks Preview -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="340" />
                <ColumnDefinition Width="14" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Student Summary Card -->
            <Border Grid.Column="0" Background="#FAFAFA" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="14">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="مشخصات و خلاصه وضعیت" FontSize="16" FontWeight="Bold" Foreground="#1E1B4B" Margin="0,0,0,12" />

                        <Border Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock x:Name="StudentNameTextBlock" Text="نام شاگرد: -" FontWeight="Bold" FontSize="14" Foreground="#1E293B" Margin="0,0,0,4" />
                                <TextBlock x:Name="FatherNameTextBlock" Text="نام پدر: -" Foreground="#475569" Margin="0,0,0,4" />
                                <TextBlock x:Name="ClassNameTextBlock" Text="صنف: -" Foreground="#475569" Margin="0,0,0,4" />
                                <TextBlock x:Name="RollNumberTextBlock" Text="شماره اساس: -" Foreground="#475569" />
                            </StackPanel>
                        </Border>

                        <Border Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Text="آمار نتایج امتحانات:" FontWeight="Bold" Foreground="#334155" Margin="0,0,0,6" />
                                <TextBlock x:Name="TotalScoreTextBlock" Text="مجموع نمرات: ۰ / ۰" Margin="0,0,0,4" />
                                <TextBlock x:Name="AveragePercentageTextBlock" Text="اوسط فیصدی: ۰%" FontWeight="SemiBold" Margin="0,0,0,4" />
                                <TextBlock x:Name="PassedFailedTextBlock" Text="کامیاب: ۰ | ناکام: ۰" Margin="0,0,0,4" />
                            </StackPanel>
                        </Border>

                        <!-- Promotion Badge -->
                        <Border x:Name="PromotionBadgeBorder" Background="#F1F5F9" BorderBrush="#CBD5E1" BorderThickness="1.5" CornerRadius="6" Padding="10" Margin="0,0,0,14">
                            <StackPanel>
                                <TextBlock x:Name="PromotionStatusTitleTextBlock" Text="وضعیت ارتقاء: نامشخص" FontWeight="Bold" FontSize="14" Foreground="#475569" />
                                <TextBlock x:Name="PromotionReasonTextBlock" Text="" FontSize="12" Foreground="#DC2626" Margin="0,4,0,0" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <!-- PDF Actions -->
                        <Button x:Name="GenerateSinglePdfButton" Height="38" Content="📄 صدور کارنامه PDF شاگرد" Background="#4338CA" Foreground="White" FontWeight="Bold" FontSize="13" Margin="0,0,0,8" Click="GenerateSinglePdfButton_Click" />
                        <Button x:Name="GenerateClassPdfButton" Height="38" Content="📑 صدور کارنامه‌های کل صنف (دسته‌جمعی)" Background="#0D9488" Foreground="White" FontWeight="Bold" FontSize="13" Margin="0,0,0,8" Click="GenerateClassPdfButton_Click" />
                        <Button x:Name="OpenGeneratedPdfButton" Height="34" Content="👁️ مشاهده و چاپ آخرین PDF" Background="#64748B" Foreground="White" FontWeight="SemiBold" Click="OpenGeneratedPdfButton_Click" IsEnabled="False" />
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Marks Table Preview -->
            <Border Grid.Column="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="پیش‌نمایش جدول نمرات شاگرد" FontWeight="Bold" FontSize="15" Foreground="#334155" Margin="4,4,4,8" />

                    <DataGrid x:Name="PreviewMarksDataGrid"
                              Grid.Row="1"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              GridLinesVisibility="All"
                              HorizontalGridLinesBrush="#E2E8F0"
                              VerticalGridLinesBrush="#E2E8F0"
                              RowHeight="32">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="نام مضمون" Binding="{Binding SubjectName}" Width="*" />
                            <DataGridTextColumn Header="چهارونیم‌ماهه (۴۰)" Binding="{Binding MidtermScore, StringFormat={}{0:0.##}}" Width="120" />
                            <DataGridTextColumn Header="سالانه (۶۰)" Binding="{Binding FinalScore, StringFormat={}{0:0.##}}" Width="100" />
                            <DataGridTextColumn Header="مجموع (۱۰۰)" Binding="{Binding TotalScore, StringFormat={}{0:0.##}}" FontWeight="Bold" Width="100" />
                            <DataGridTextColumn Header="نتیجه" Binding="{Binding IsPassText}" FontWeight="Bold" Width="80" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <TextBlock Grid.Row="2" Margin="4,8,4,4" Text="💡 پس از اطمینان از صحت نمرات، می‌توانید فایل PDF رسمی را جهت چاپ تولید نمایید." FontSize="12" Foreground="#64748B" />
                </Grid>
            </Border>
        </Grid>

        <!-- Status Bottom Bar -->
        <Border Grid.Row="3" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" Padding="10" CornerRadius="6" Margin="0,10,0,0">
            <TextBlock x:Name="StatusTextBlock" VerticalAlignment="Center" FontWeight="Bold" FontSize="13" Foreground="#16A34A" Text="" />
        </Border>
    </Grid>
</UserControl>
