<UserControl x:Class="Maktab.App.Wpf.Views.AlertsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             FlowDirection="RightToLeft">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#B45309" Padding="14" CornerRadius="6" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="🔔 اعلان‌ها" FontSize="20" FontWeight="Bold" Foreground="White" />
                <TextBlock Text=" (Alerts Center)" FontSize="13" Foreground="#FDE68A" VerticalAlignment="Center" Margin="10,2,0,0" />
            </StackPanel>
        </Border>

        <!-- Alerts List -->
        <Border Grid.Row="1" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="لیست اعلان‌ها" FontWeight="Bold" FontSize="15" Foreground="#334155" />
                    <Button Grid.Column="1" Content="🔄 تازه‌سازی" Height="28" Padding="10,0"
                            Background="#3B82F6" Foreground="White" FontWeight="SemiBold"
                            Click="RefreshButton_Click" />
                </Grid>

                <DataGrid x:Name="AlertsDataGrid"
                          Grid.Row="1"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#E2E8F0"
                          RowHeight="32">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="نوع" Binding="{Binding Type}" Width="120" />
                        <DataGridTextColumn Header="پیام" Binding="{Binding Message}" Width="*" />
                        <DataGridTextColumn Header="سطح" Binding="{Binding Severity}" Width="80" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
