<UserControl x:Class="Maktab.App.Wpf.Views.StudentManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             FlowDirection="RightToLeft">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#1E3A8A" Padding="14" CornerRadius="6" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="👨‍🎓 مدیریت و ثبت‌نام شاگردان" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" />
                <TextBlock Text=" (Student Registration &amp; Records)" FontSize="13" Foreground="#93C5FD" VerticalAlignment="Center" Margin="10,2,0,0" />
            </StackPanel>
        </Border>

        <!-- Filter & Search Bar -->
        <Border Grid.Row="1" Background="#F1F5F9" BorderBrush="#CBD5E1" BorderThickness="1" Padding="10" CornerRadius="6" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="فیلتر بر اساس صنف:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,0" />
                <ComboBox x:Name="FilterClassComboBox" Grid.Column="1" Height="32" SelectionChanged="FilterClassComboBox_SelectionChanged" DisplayMemberPath="GradeName" SelectedValuePath="ClassId" />

                <TextBlock Grid.Column="3" Text="جستجو (نام / نمره اساس):" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,0" />
                <TextBox x:Name="SearchTextBox" Grid.Column="4" Height="32" VerticalContentAlignment="Center" TextChanged="SearchTextBox_TextChanged" Margin="0,0,10,0" />

                <Button Grid.Column="5" Content="🔄 تازه‌سازی لیست" Height="32" Padding="12,0" Background="#3B82F6" Foreground="White" FontWeight="SemiBold" Click="RefreshButton_Click" />
            </Grid>
        </Border>

        <!-- Main Content Area: Form & DataGrid -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="340" />
                <ColumnDefinition Width="14" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Student Form -->
            <Border Grid.Column="0" Background="#FAFAFA" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="14">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="مشخصات شاگرد" FontSize="16" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,12" />

                        <TextBlock Text="نام (First Name) *" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBox x:Name="FirstNameTextBox" Height="32" VerticalContentAlignment="Center" Margin="0,0,0,10" MaxLength="50" />

                        <TextBlock Text="تخلص (Last Name) *" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBox x:Name="LastNameTextBox" Height="32" VerticalContentAlignment="Center" Margin="0,0,0,10" MaxLength="50" />

                        <TextBlock Text="نام پدر (Father's Name) *" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBox x:Name="FatherNameTextBox" Height="32" VerticalContentAlignment="Center" Margin="0,0,0,10" MaxLength="50" />

                        <TextBlock Text="صنف (Class/Grade) *" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <ComboBox x:Name="FormClassComboBox" Height="32" Margin="0,0,0,10" DisplayMemberPath="GradeName" SelectedValuePath="ClassId" />

                        <TextBlock Text="شماره اساس / رول نمبر (Roll No.) *" FontWeight="SemiBold" Margin="0,0,0,4" />
                        <TextBox x:Name="RollNumberTextBox" Height="32" VerticalContentAlignment="Center" Margin="0,0,0,16" MaxLength="20" />

                        <!-- Action Buttons -->
                        <UniformGrid Columns="2" Rows="2" Margin="0,0,0,10">
                            <Button Margin="0,0,4,8" Height="36" Content="➕ ثبت شاگرد" Background="#16A34A" Foreground="White" FontWeight="Bold" Click="AddStudentButton_Click" />
                            <Button Margin="4,0,0,8" Height="36" Content="✏️ ویرایش" Background="#D97706" Foreground="White" FontWeight="Bold" Click="UpdateStudentButton_Click" />
                            <Button Margin="0,4,4,0" Height="36" Content="🗑️ حذف" Background="#DC2626" Foreground="White" FontWeight="Bold" Click="DeleteStudentButton_Click" />
                            <Button Margin="4,4,0,0" Height="36" Content="🧹 پاک کردن" Background="#64748B" Foreground="White" FontWeight="SemiBold" Click="ClearFormButton_Click" />
                        </UniformGrid>

                        <TextBlock x:Name="FormStatusTextBlock" Text="" Foreground="#059669" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,8,0,0" />
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Students DataGrid -->
            <Border Grid.Column="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="4,4,4,8">
                        <TextBlock Text="لیست شاگردان ثبت‌شده" FontWeight="Bold" FontSize="15" Foreground="#334155" />
                        <TextBlock x:Name="CountTextBlock" Text=" (تعداد: 0)" Foreground="#64748B" Margin="6,2,0,0" />
                    </StackPanel>

                    <DataGrid x:Name="StudentsDataGrid"
                              Grid.Row="1"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="#E2E8F0"
                              SelectionChanged="StudentsDataGrid_SelectionChanged"
                              RowHeight="32">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="آیدی" Binding="{Binding StudentId}" Width="60" />
                            <DataGridTextColumn Header="شماره اساس" Binding="{Binding RollNumber}" Width="100" />
                            <DataGridTextColumn Header="نام" Binding="{Binding FirstName}" Width="*" />
                            <DataGridTextColumn Header="تخلص" Binding="{Binding LastName}" Width="*" />
                            <DataGridTextColumn Header="نام پدر" Binding="{Binding FatherName}" Width="*" />
                            <DataGridTextColumn Header="صنف" Binding="{Binding ClassName}" Width="100" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <TextBlock Grid.Row="2" Margin="4,8,4,4" Text="💡 برای ویرایش یا حذف، روی ردیف شاگرد کلیک کنید." FontSize="12" Foreground="#64748B" />
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
