<UserControl x:Class="Maktab.App.Wpf.Views.PromotionHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             FlowDirection="RightToLeft">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#334155" Padding="14" CornerRadius="6" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="📜 تاریخچه ارتقاء" FontSize="20" FontWeight="Bold" Foreground="White" />
                <TextBlock Text=" (Promotion History)" FontSize="13" Foreground="#CBD5E1" VerticalAlignment="Center" Margin="10,2,0,0" />
            </StackPanel>
        </Border>

        <!-- Filter Bar -->
        <Border Grid.Row="1" Background="#F1F5F9" BorderBrush="#CBD5E1" BorderThickness="1" Padding="10" CornerRadius="6" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="سال تعلیمی:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,8,0" />
                <ComboBox x:Name="AcademicYearComboBox" Grid.Column="1" Height="32"
                          DisplayMemberPath="YearName" SelectedValuePath="AcademicYearId"
                          SelectionChanged="AcademicYearComboBox_SelectionChanged" />

                <Button Grid.Column="3" Content="🔄 تازه‌سازی" Height="32" Padding="12,0"
                        Background="#3B82F6" Foreground="White" FontWeight="SemiBold"
                        Click="RefreshButton_Click" />
            </Grid>
        </Border>

        <!-- History DataGrid -->
        <Border Grid.Row="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="8">
            <DataGrid x:Name="HistoryDataGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#E2E8F0"
                      RowHeight="32">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="شاگرد" Binding="{Binding StudentName}" Width="*" />
                    <DataGridTextColumn Header="اساس" Binding="{Binding RollNumber}" Width="80" />
                    <DataGridTextColumn Header="از صنف" Binding="{Binding FromClassName}" Width="100" />
                    <DataGridTextColumn Header="به صنف" Binding="{Binding ToClassName}" Width="100" />
                    <DataGridTextColumn Header="سال تعلیمی" Binding="{Binding AcademicYearName}" Width="120" />
                    <DataGridTextColumn Header="نتیجه" Binding="{Binding Result}" Width="100" />
                    <DataGridTextColumn Header="تاریخ" Binding="{Binding PromotionDate, StringFormat={}{0:yyyy/MM/dd HH:mm}}" Width="140" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
