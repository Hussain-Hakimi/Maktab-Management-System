<UserControl x:Class="Maktab.App.Wpf.Views.FeesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1000"
             FlowDirection="RightToLeft">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#047857" Padding="14" CornerRadius="6" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="💰 مدیریت فیس و پرداخت‌ها" FontSize="20" FontWeight="Bold" Foreground="White" />
                <TextBlock Text=" (Fees &amp; Payments Management)" FontSize="13" Foreground="#A7F3D0" VerticalAlignment="Center" Margin="10,2,0,0" />
            </StackPanel>
        </Border>

        <!-- Main Tabs -->
        <TabControl Grid.Row="1" BorderThickness="0" Background="Transparent">
            <!-- Fees Tab -->
            <TabItem Header="📋 فیس‌ها">
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="300" />
                        <ColumnDefinition Width="14" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Fee Form -->
                    <Border Grid.Column="0" Background="#FAFAFA" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="14">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Text="ثبت فیس جدید" FontSize="16" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,12" />

                                <TextBlock Text="شاگرد *" FontWeight="SemiBold" Margin="0,0,0,4" />
                                <ComboBox x:Name="FeeStudentComboBox" Height="32" Margin="0,0,0,10" DisplayMemberPath="DisplayName" SelectedValuePath="StudentId" />

                                <TextBlock Text="نوع فیس (Fee Type) *" FontWeight="SemiBold" Margin="0,0,0,4" />
                                <TextBox x:Name="FeeTypeTextBox" Height="32" Margin="0,0,0,10" MaxLength="50" />

                                <TextBlock Text="مبلغ (Amount) *" FontWeight="SemiBold" Margin="0,0,0,4" />
                                <TextBox x:Name="FeeAmountTextBox" Height="32" Margin="0,0,0,10" MaxLength="10" />

                                <TextBlock Text="تاریخ سررسید (Due Date) *" FontWeight="SemiBold" Margin="0,0,0,4" />
                                <DatePicker x:Name="FeeDueDatePicker" Height="32" Margin="0,0,0,16" />

                                <Button Height="36" Content="➕ ثبت فیس" Background="#16A34A" Foreground="White" FontWeight="Bold" Click="AddFeeButton_Click" />
                            </StackPanel>
                        </ScrollViewer>
                    </Border>

                    <!-- Fees DataGrid -->
                    <Border Grid.Column="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="8">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0" Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="لیست فیس‌ها" FontWeight="Bold" FontSize="15" Foreground="#334155" />
                                <Button Grid.Column="1" Content="🔄 تازه‌سازی" Height="28" Padding="10,0" Background="#3B82F6" Foreground="White" FontWeight="SemiBold" Click="RefreshFeesButton_Click" Margin="0,0,8,0" />
                                <Button Grid.Column="2" Content="🗑️ حذف فیس" Height="28" Padding="10,0" Background="#DC2626" Foreground="White" FontWeight="SemiBold" Click="DeleteFeeButton_Click" />
                            </Grid>

                            <DataGrid x:Name="FeesDataGrid"
                                      Grid.Row="1"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      IsReadOnly="True"
                                      SelectionMode="Single"
                                      GridLinesVisibility="Horizontal"
                                      HorizontalGridLinesBrush="#E2E8F0"
                                      RowHeight="32">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Fee ID" Binding="{Binding FeeId}" Width="70" />
                                    <DataGridTextColumn Header="شاگرد" Binding="{Binding StudentName}" Width="*" />
                                    <DataGridTextColumn Header="اساس" Binding="{Binding RollNumber}" Width="70" />
                                    <DataGridTextColumn Header="نوع فیس" Binding="{Binding FeeType}" Width="120" />
                                    <DataGridTextColumn Header="مبلغ" Binding="{Binding Amount, StringFormat={}{0:N0}}" Width="90" />
                                    <DataGridTextColumn Header="پرداخت‌شده" Binding="{Binding TotalPaid, StringFormat={}{0:N0}}" Width="90" />
                                    <DataGridTextColumn Header="باقی‌مانده" Binding="{Binding Outstanding, StringFormat={}{0:N0}}" Width="90" />
                                    <DataGridTextColumn Header="سررسید" Binding="{Binding DueDate, StringFormat={}{0:yyyy/MM/dd}}" Width="90" />
                                    <DataGridTextColumn Header="وضعیت" Binding="{Binding Status}" Width="80" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Payments Tab -->
            <TabItem Header="💳 پرداخت‌ها">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Record Payment Form -->
                    <Border Grid.Row="0" Background="#F0FDF4" BorderBrush="#BBF7D0" BorderThickness="1" Padding="10" CornerRadius="6" Margin="0,0,0,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="200" />
                                <ColumnDefinition Width="20" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="20" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="160" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="انتخاب فیس:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,6,0" />
                            <ComboBox x:Name="PaymentFeeComboBox" Grid.Column="1" Height="32" DisplayMemberPath="FeeDisplay" SelectedValuePath="FeeId" />

                            <TextBlock Grid.Column="3" Text="مبلغ پرداخت:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,6,0" />
                            <TextBox x:Name="PaymentAmountTextBox" Grid.Column="4" Height="32" VerticalContentAlignment="Center" />

                            <TextBlock Grid.Column="6" Text="تاریخ پرداخت:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,6,0" />
                            <DatePicker x:Name="PaymentDatePicker" Grid.Column="7" Height="32" />

                            <Button Grid.Column="9" Content="💳 ثبت پرداخت" Height="32" Padding="16,0" Background="#16A34A" Foreground="White" FontWeight="Bold" Click="RecordPaymentButton_Click" />
                        </Grid>
                    </Border>

                    <!-- Payments DataGrid -->
                    <Border Grid.Row="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="8">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0" Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="سوابق پرداخت‌ها" FontWeight="Bold" FontSize="15" Foreground="#334155" />
                                <Button Grid.Column="1" Content="🔄 تازه‌سازی" Height="28" Padding="10,0" Background="#3B82F6" Foreground="White" FontWeight="SemiBold" Click="RefreshPaymentsButton_Click" />
                            </Grid>

                            <DataGrid x:Name="PaymentsDataGrid"
                                      Grid.Row="1"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      IsReadOnly="True"
                                      SelectionMode="Single"
                                      GridLinesVisibility="Horizontal"
                                      HorizontalGridLinesBrush="#E2E8F0"
                                      RowHeight="32">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Payment ID" Binding="{Binding PaymentId}" Width="80" />
                                    <DataGridTextColumn Header="شاگرد" Binding="{Binding StudentName}" Width="*" />
                                    <DataGridTextColumn Header="نوع فیس" Binding="{Binding FeeType}" Width="120" />
                                    <DataGridTextColumn Header="مبلغ" Binding="{Binding Amount, StringFormat={}{0:N0}}" Width="90" />
                                    <DataGridTextColumn Header="تاریخ" Binding="{Binding PaymentDate, StringFormat={}{0:yyyy/MM/dd}}" Width="90" />
                                    <DataGridTextColumn Header="رسید" Binding="{Binding ReceiptNumber}" Width="150" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
