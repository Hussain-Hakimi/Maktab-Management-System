<Window x:Class="Maktab.App.Wpf.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ورود به سیستم"
        Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Language="fa-IR"
        FlowDirection="RightToLeft">

    <Grid Background="#F1F5F9">
        <Border Background="White" CornerRadius="8" Margin="20" Padding="20">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="📘 سیستم مدیریت مکتب" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10" />
                <TextBlock Text="لطفاً وارد شوید" FontSize="14" Foreground="#475569" HorizontalAlignment="Center" Margin="0,0,0,20" />

                <TextBlock Text="نام کاربری" FontWeight="SemiBold" Margin="0,0,0,4" />
                <TextBox x:Name="UsernameTextBox" Height="32" Margin="0,0,0,10" />

                <TextBlock Text="رمز عبور" FontWeight="SemiBold" Margin="0,0,0,4" />
                <PasswordBox x:Name="PasswordBox" Height="32" Margin="0,0,0,20" />

                <Button Height="38" Content="ورود" Background="#3B82F6" Foreground="White" FontWeight="Bold" Click="LoginButton_Click" />

                <TextBlock x:Name="ErrorTextBlock" Foreground="#DC2626" FontSize="12" Margin="0,10,0,0" TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
