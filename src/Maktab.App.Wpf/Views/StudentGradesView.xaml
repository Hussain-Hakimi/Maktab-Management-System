<UserControl x:Class="Maktab.App.Wpf.Views.StudentGradesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="900"
             FlowDirection="RightToLeft">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#0F766E" Padding="14" CornerRadius="6" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="🎓 نمرات شاگرد (همه مضامین)" FontSize="20" FontWeight="Bold" Foreground="White" />
                <TextBlock Text=" (Student-Centric Grade View)" FontSize="13" Foreground="#99F6E4" VerticalAlignment="Center" Margin="10,2,0,0" />
            </StackPanel>
        </Border>

        <!-- Selection Panel -->
        <Border Grid.Row="1" Background="#F0FDFA" BorderBrush="#99F6E4" BorderThickness="1" Padding="10" CornerRadius="6" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="180" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="180" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="صنف:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,6,0" />
                <ComboBox x:Name="ClassComboBox" Grid.Column="1" Height="32" DisplayMemberPath="GradeName" SelectedValuePath="ClassId" SelectionChanged="ClassComboBox_SelectionChanged" />

                <TextBlock Grid.Column="3" Text="شاگرد:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,6,0" />
                <ComboBox x:Name="StudentComboBox" Grid.Column="4" Height="32" DisplayMemberPath="DisplayName" SelectedValuePath="StudentId" SelectionChanged="StudentComboBox_SelectionChanged" />

                <TextBlock Grid.Column="6" Text="سال تعلیمی:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,6,0" />
                <ComboBox x:Name="AcademicYearComboBox" Grid.Column="7" Height="32" DisplayMemberPath="YearName" SelectedValuePath="AcademicYearId" SelectionChanged="AcademicYearComboBox_SelectionChanged" />

                <Button Grid.Column="9" Content="🔄 بارگذاری مجدد" Height="32" Padding="12,0" Background="#0D9488" Foreground="White" FontWeight="SemiBold" Click="ReloadButton_Click" />
            </Grid>
        </Border>

        <!-- Marks DataGrid -->
        <Border Grid.Row="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <DataGrid x:Name="GradesDataGrid"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          SelectionMode="Single"
                          GridLinesVisibility="All"
                          HorizontalGridLinesBrush="#E2E8F0"
                          VerticalGridLinesBrush="#E2E8F0"
                          RowHeight="34"
                          CellEditEnding="GradesDataGrid_CellEditEnding">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="مضمون" Binding="{Binding SubjectName}" IsReadOnly="True" Width="*" />
                        <DataGridTextColumn Header="چهارونیم‌ماهه (۰-۴۰)" Binding="{Binding MidtermScore, StringFormat={}{0:0.##}, UpdateSourceTrigger=PropertyChanged}" Width="150" />
                        <DataGridTextColumn Header="سالانه (۰-۶۰)" Binding="{Binding FinalScore, StringFormat={}{0:0.##}, UpdateSourceTrigger=PropertyChanged}" Width="140" />
                        <DataGridTextColumn Header="مجموع (۱۰۰)" Binding="{Binding TotalScore, StringFormat={}{0:0.##}}" IsReadOnly="True" FontWeight="Bold" Width="100" />
                        <DataGridTextColumn Header="نتیجه" Binding="{Binding ResultText}" IsReadOnly="True" FontWeight="Bold" Width="90" />
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock Grid.Row="1" Margin="4,8,4,4" Text="💡 نمرات را مستقیماً در جدول ویرایش کنید و سپس دکمه ذخیره را بزنید." FontSize="12" Foreground="#64748B" />
            </Grid>
        </Border>

        <!-- Bottom Action Bar -->
        <Border Grid.Row="3" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" Padding="10" CornerRadius="6" Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="SaveStatusTextBlock" VerticalAlignment="Center" FontWeight="Bold" FontSize="14" Foreground="#16A34A" Text="" />

                <Button x:Name="SaveButton" Grid.Column="1" Content="💾 ذخیره نمرات" Height="38" Padding="20,0" Background="#16A34A" Foreground="White" FontWeight="Bold" FontSize="14" Click="SaveButton_Click" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
