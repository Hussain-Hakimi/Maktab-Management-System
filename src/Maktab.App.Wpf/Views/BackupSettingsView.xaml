<UserControl x:Class="Maktab.App.Wpf.Views.BackupSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="1000"
             FlowDirection="RightToLeft">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#047857" Padding="14" CornerRadius="6" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="💾 پشتیبان‌گیری، بازیابی و وقایع سیستم" FontSize="20" FontWeight="Bold" Foreground="White" />
                    <TextBlock Text=" (Database Backup, Restore &amp; Diagnostics)" FontSize="13" Foreground="#A7F3D0" VerticalAlignment="Center" Margin="10,2,0,0" />
                </StackPanel>
                <Button Grid.Column="1" Content="📂 باز کردن پوشه دیتابیس و پشتیبان‌ها" Background="#065F46" Foreground="White" FontWeight="SemiBold" Padding="12,6" Click="OpenBackupsFolderButton_Click" />
            </Grid>
        </Border>

        <!-- Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360" />
                <ColumnDefinition Width="14" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left Controls & Info -->
            <StackPanel Grid.Column="0">
                <!-- Backup Status Card -->
                <Border Background="#FAFAFA" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="14" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="عملیات پشتیبان‌گیری دیتابیس" FontSize="15" FontWeight="Bold" Foreground="#064E3B" Margin="0,0,0,10" />

                        <Border Background="#ECFDF5" BorderBrush="#A7F3D0" BorderThickness="1" CornerRadius="4" Padding="8" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="✅ پایداری در برابر قطع برق: فعال (SQLite WAL)" FontSize="12" FontWeight="SemiBold" Foreground="#047857" />
                                <TextBlock Text="✅ سیاست نگهداری فایل‌ها: ۷ روز اخیر" FontSize="12" Foreground="#065F46" Margin="0,4,0,0" />
                                <TextBlock Text="✅ محل ذخیره: AppData/Backups" FontSize="12" Foreground="#065F46" Margin="0,2,0,0" />
                            </StackPanel>
                        </Border>

                        <Button Height="40" Content="📦 تهیه نسخه پشتیبان دستی هم‌اکنون" Background="#059669" Foreground="White" FontWeight="Bold" FontSize="13" Margin="0,0,0,8" Click="CreateBackupButton_Click" />
                        <Button Height="40" Content="📂 کپی به فلش USB" Background="#0284C7" Foreground="White" FontWeight="Bold" FontSize="13" Margin="0,0,0,8" Click="CopyBackupToUsbButton_Click" />
                    </StackPanel>
                </Border>

                <!-- Restore Operations Card -->
                <Border Background="#FAFAFA" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="14" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="بازیابی اطلاعات (Restore)" FontSize="15" FontWeight="Bold" Foreground="#991B1B" Margin="0,0,0,10" />

                        <TextBlock Text="⚠️ هنگام بازیابی، اطلاعات دیتابیس فعلی با فایل پشتیبان انتخابی جایگزین خواهد شد." FontSize="12" Foreground="#DC2626" TextWrapping="Wrap" Margin="0,0,0,12" />

                        <Button x:Name="RestoreSelectedButton" Height="36" Content="🔄 بازیابی از فایل انتخاب‌شده در جدول" Background="#DC2626" Foreground="White" FontWeight="Bold" FontSize="12" Margin="0,0,0,8" Click="RestoreSelectedButton_Click" />
                        <Button Height="34" Content="📁 انتخاب فایل دیتابیس دیگر..." Background="#475569" Foreground="White" FontWeight="SemiBold" FontSize="12" Click="BrowseAndRestoreButton_Click" />
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Right Tables & Logs Tabs -->
            <Border Grid.Column="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="8">
                <TabControl BorderThickness="0" Background="Transparent">
                    <!-- Tab 1: Backups List -->
                    <TabItem Header="📦 لیست نسخه‌های پشتیبان (Backups)">
                        <Grid Margin="0,10,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0" Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="فایل‌های پشتیبان موجود در سیستم:" FontWeight="Bold" Foreground="#334155" VerticalAlignment="Center" />
                                <Button Grid.Column="1" Content="🔄 تازه‌سازی لیست" Height="28" Padding="10,0" Background="#3B82F6" Foreground="White" FontWeight="SemiBold" Click="RefreshBackupsButton_Click" />
                            </Grid>

                            <DataGrid x:Name="BackupsDataGrid"
                                      Grid.Row="1"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      IsReadOnly="True"
                                      SelectionMode="Single"
                                      GridLinesVisibility="Horizontal"
                                      HorizontalGridLinesBrush="#E2E8F0"
                                      RowHeight="32">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="نام فایل پشتیبان" Binding="{Binding FileName}" Width="*" />
                                    <DataGridTextColumn Header="تاریخ و زمان ایجاد" Binding="{Binding CreatedAtFormatted}" Width="160" />
                                    <DataGridTextColumn Header="حجم فایل" Binding="{Binding FileSizeFormatted}" Width="100" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </TabItem>

                    <!-- Tab 2: System Logs -->
                    <TabItem Header="📋 وقایع و گزارش خطاهای سیستم (Logs)">
                        <Grid Margin="0,10,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0" Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="آخرین وقایع و خطاهای ثبت شده در Logs/app.log:" FontWeight="Bold" Foreground="#334155" VerticalAlignment="Center" />
                                <Button Grid.Column="1" Content="🔄 تازه‌سازی لاگ‌ها" Height="28" Padding="10,0" Background="#3B82F6" Foreground="White" FontWeight="SemiBold" Click="RefreshLogsButton_Click" />
                            </Grid>

                            <TextBox x:Name="LogsTextBox"
                                     Grid.Row="1"
                                     IsReadOnly="True"
                                     VerticalScrollBarVisibility="Auto"
                                     HorizontalScrollBarVisibility="Auto"
                                     FontFamily="Consolas, Courier New"
                                     FontSize="12"
                                     Background="#0F172A"
                                     Foreground="#38BDF8"
                                     Padding="8"
                                     FlowDirection="LeftToRight" />
                        </Grid>
                    </TabItem>
                </TabControl>
            </Border>
        </Grid>

        <!-- Status Bottom Bar -->
        <Border Grid.Row="2" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" Padding="10" CornerRadius="6" Margin="0,10,0,0">
            <TextBlock x:Name="StatusTextBlock" VerticalAlignment="Center" FontWeight="Bold" FontSize="13" Foreground="#047857" Text="" />
        </Border>
    </Grid>
</UserControl>
