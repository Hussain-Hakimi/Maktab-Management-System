<UserControl x:Class="Maktab.App.Wpf.Views.SchoolSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             FlowDirection="RightToLeft">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#334155" Padding="14" CornerRadius="6" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="🏫 تنظیمات عمومی مکتب" FontSize="20" FontWeight="Bold" Foreground="White" />
                <TextBlock Text=" (School Settings)" FontSize="13" Foreground="#CBD5E1" VerticalAlignment="Center" Margin="10,2,0,0" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="20">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="اطلاعات عمومی" FontSize="18" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,20" />

                    <!-- Government Title -->
                    <TextBlock Text="عنوان دولت / حکومت" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <TextBox x:Name="GovernmentTitleTextBox" Height="34" Margin="0,0,0,12" />

                    <!-- Provincial Education -->
                    <TextBlock Text="ریاست معارف ولایت (مثال: ریاست معارف ولایت غزنی)" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <TextBox x:Name="ProvincialEducationHeaderTextBox" Height="34" Margin="0,0,0,12" />

                    <!-- District Education -->
                    <TextBlock Text="مدیریت معارف ولسوالی" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <TextBox x:Name="DistrictEducationHeaderTextBox" Height="34" Margin="0,0,0,12" />

                    <!-- School Name -->
                    <TextBlock Text="نام مکتب *" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <TextBox x:Name="SchoolNameTextBox" Height="34" Margin="0,0,0,12" />

                    <!-- School Address -->
                    <TextBlock Text="آدرس مکتب" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <TextBox x:Name="SchoolAddressTextBox" Height="34" Margin="0,0,0,12" />

                    <!-- Phone -->
                    <TextBlock Text="شماره تماس" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <TextBox x:Name="PhoneNumberTextBox" Height="34" Margin="0,0,0,12" />

                    <!-- Academic Year -->
                    <TextBlock Text="سال تعلیمی" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <TextBox x:Name="AcademicYearTextBox" Height="34" Margin="0,0,0,16" />

                    <!-- School Logo -->
                    <TextBlock Text="لوگو / مهر مکتب" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="8" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="SchoolLogoPathTextBox" Grid.Column="0" Height="34" IsReadOnly="True" />
                        <Button Grid.Column="2" Content="📁 انتخاب تصویر" Height="34" Padding="10,0" Click="BrowseLogoButton_Click" />
                    </Grid>
                    <Image x:Name="LogoPreviewImage" MaxHeight="80" MaxWidth="160" Stretch="Uniform" HorizontalAlignment="Right" Margin="0,0,0,16" />

                    <Button Height="40" Content="💾 ذخیره تنظیمات" Background="#16A34A" Foreground="White" FontWeight="Bold" FontSize="15"
                            BorderThickness="0" Cursor="Hand" Click="SaveButton_Click" HorizontalAlignment="Left" Padding="20,0" />
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
