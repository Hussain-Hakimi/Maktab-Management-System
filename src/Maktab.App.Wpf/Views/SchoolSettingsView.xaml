<UserControl x:Class="Maktab.App.Wpf.Views.SchoolSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             FlowDirection="RightToLeft">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#334155" Padding="14" CornerRadius="6" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="🏫 تنظیمات عمومی مکتب" FontSize="20" FontWeight="Bold" Foreground="White" />
                <TextBlock Text=" (School Settings)" FontSize="13" Foreground="#CBD5E1" VerticalAlignment="Center" Margin="10,2,0,0" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="20">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="اطلاعات عمومی" FontSize="18" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,20" />

                    <TextBlock Text="نام مکتب *" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <TextBox x:Name="SchoolNameTextBox" Height="36" Margin="0,0,0,16" FontSize="14" Padding="8,6" />

                    <TextBlock Text="آدرس مکتب" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <TextBox x:Name="SchoolAddressTextBox" Height="36" Margin="0,0,0,16" FontSize="14" Padding="8,6" />

                    <TextBlock Text="شماره تماس" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <TextBox x:Name="PhoneNumberTextBox" Height="36" Margin="0,0,0,16" FontSize="14" Padding="8,6" />

                    <TextBlock Text="سال تعلیمی" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <TextBox x:Name="AcademicYearTextBox" Height="36" Margin="0,0,0,16" FontSize="14" Padding="8,6" />

                    <TextBlock Text="لوگوی مکتب" FontWeight="SemiBold" Margin="0,0,0,6" />
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="LogoPathTextBox" Height="36" FontSize="14" Padding="8,6" IsReadOnly="True" VerticalContentAlignment="Center" />
                        <Button Grid.Column="1" Content="📁 انتخاب تصویر" Height="36" Margin="8,0,0,0" Padding="12,0"
                                Background="#3B82F6" Foreground="White" FontWeight="SemiBold" Click="BrowseLogoButton_Click" />
                    </Grid>

                    <Button Height="40" Content="💾 ذخیره تنظیمات" Background="#16A34A" Foreground="White" FontWeight="Bold" FontSize="15"
                            CornerRadius="6" BorderThickness="0" Cursor="Hand" Click="SaveButton_Click" HorizontalAlignment="Left" Padding="20,0" />
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
