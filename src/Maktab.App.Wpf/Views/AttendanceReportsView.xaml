<UserControl x:Class="Maktab.App.Wpf.Views.AttendanceReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             FlowDirection="RightToLeft">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#0F766E" Padding="14" CornerRadius="6" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="🗓️ گزارش حاضری" FontSize="20" FontWeight="Bold" Foreground="White" />
                <TextBlock Text=" (Attendance Reports)" FontSize="13" Foreground="#99F6E4" VerticalAlignment="Center" Margin="10,2,0,0" />
            </StackPanel>
        </Border>

        <!-- Filters -->
        <Border Grid.Row="1" Background="#F0FDFA" BorderBrush="#99F6E4" BorderThickness="1" Padding="10" CornerRadius="6" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="180" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="180" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="صنف:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,6,0" />
                <ComboBox x:Name="ClassComboBox" Grid.Column="1" Height="32" DisplayMemberPath="GradeName" SelectedValuePath="ClassId" SelectionChanged="ClassComboBox_SelectionChanged" />

                <TextBlock Grid.Column="3" Text="سال تعلیمی:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,6,0" />
                <ComboBox x:Name="AcademicYearComboBox" Grid.Column="4" Height="32" DisplayMemberPath="YearName" SelectedValuePath="AcademicYearId" />

                <TextBlock Grid.Column="6" Text="ماه:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,6,0" />
                <ComboBox x:Name="MonthComboBox" Grid.Column="7" Height="32" />

                <Button Grid.Column="9" Content="📊 خلاصه" Height="32" Padding="12,0" Background="#2563EB" Foreground="White" FontWeight="SemiBold" Click="ShowSummaryButton_Click" Margin="0,0,8,0" />
                <Button Grid.Column="10" Content="📅 گزارش ماهانه" Height="32" Padding="12,0" Background="#16A34A" Foreground="White" FontWeight="SemiBold" Click="ShowMonthlyButton_Click" />
            </Grid>
        </Border>

        <!-- DataGrid -->
        <Border Grid.Row="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock x:Name="TitleTextBlock" Text="خلاصه حاضری" FontWeight="Bold" FontSize="15" Foreground="#334155" Margin="4,4,4,8" />
                <DataGrid x:Name="ReportDataGrid"
                          Grid.Row="1"
                          AutoGenerateColumns="True"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          GridLinesVisibility="All"
                          HorizontalGridLinesBrush="#E2E8F0"
                          VerticalGridLinesBrush="#E2E8F0"
                          RowHeight="32" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
