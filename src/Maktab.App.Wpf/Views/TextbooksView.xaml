<UserControl x:Class="Maktab.App.Wpf.Views.TextbooksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             FlowDirection="RightToLeft">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#9A3412" Padding="14" CornerRadius="6" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="📕 مدیریت کتب درسی" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" />
                <TextBlock Text=" (Textbooks — Inventory &amp; Distribution)" FontSize="13" Foreground="#FDBA74" VerticalAlignment="Center" Margin="10,2,0,0" />
            </StackPanel>
        </Border>

        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
            <!-- Tab 1: Inventory -->
            <TabItem Header="📦 انبار کتب درسی">
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="320" />
                        <ColumnDefinition Width="14" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Textbook Form -->
                    <Border Grid.Column="0" Background="#FAFAFA" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="14">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Text="مشخصات کتاب درسی" FontSize="16" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,12" />

                                <TextBlock Text="عنوان کتاب *" FontWeight="SemiBold" Margin="0,0,0,4" />
                                <TextBox x:Name="TextbookTitleTextBox" Height="32" VerticalContentAlignment="Center" Margin="0,0,0,10" MaxLength="100" />

                                <TextBlock Text="مضمون (Subject)" FontWeight="SemiBold" Margin="0,0,0,4" />
                                <TextBox x:Name="TextbookSubjectTextBox" Height="32" VerticalContentAlignment="Center" Margin="0,0,0,10" MaxLength="50" />

                                <TextBlock Text="صنف/درجه (Grade Level)" FontWeight="SemiBold" Margin="0,0,0,4" />
                                <TextBox x:Name="TextbookGradeTextBox" Height="32" VerticalContentAlignment="Center" Margin="0,0,0,10" MaxLength="50" />

                                <TextBlock Text="تعداد نسخه‌ها *" FontWeight="SemiBold" Margin="0,0,0,4" />
                                <TextBox x:Name="TextbookCopiesTextBox" Height="32" VerticalContentAlignment="Center" Margin="0,0,0,16" />

                                <UniformGrid Columns="2" Rows="2" Margin="0,0,0,10">
                                    <Button Margin="0,0,4,8" Height="36" Content="➕ ثبت کتاب" Background="#16A34A" Foreground="White" FontWeight="Bold" Click="AddTextbookButton_Click" />
                                    <Button Margin="4,0,0,8" Height="36" Content="✏️ ویرایش" Background="#D97706" Foreground="White" FontWeight="Bold" Click="UpdateTextbookButton_Click" />
                                    <Button Margin="0,4,4,0" Height="36" Content="🗑️ حذف" Background="#DC2626" Foreground="White" FontWeight="Bold" Click="DeleteTextbookButton_Click" />
                                    <Button Margin="4,4,0,0" Height="36" Content="🧹 پاک کردن" Background="#64748B" Foreground="White" FontWeight="SemiBold" Click="ClearTextbookFormButton_Click" />
                                </UniformGrid>

                                <TextBlock x:Name="TextbookFormStatusTextBlock" Text="" Foreground="#059669" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,8,0,0" />
                            </StackPanel>
                        </ScrollViewer>
                    </Border>

                    <!-- Textbooks DataGrid -->
                    <Border Grid.Column="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="8">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <StackPanel Orientation="Horizontal" Margin="4,4,4,8">
                                <TextBlock Text="لیست کتب درسی" FontWeight="Bold" FontSize="15" Foreground="#334155" />
                                <TextBlock x:Name="TextbooksCountTextBlock" Text=" (تعداد: 0)" Foreground="#64748B" Margin="6,2,0,0" />
                            </StackPanel>
                            <DataGrid x:Name="TextbooksDataGrid"
                                      Grid.Row="1"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      IsReadOnly="True"
                                      SelectionMode="Single"
                                      GridLinesVisibility="Horizontal"
                                      HorizontalGridLinesBrush="#E2E8F0"
                                      SelectionChanged="TextbooksDataGrid_SelectionChanged"
                                      RowHeight="32">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="آیدی" Binding="{Binding TextbookId}" Width="60" />
                                    <DataGridTextColumn Header="عنوان" Binding="{Binding Title}" Width="*" />
                                    <DataGridTextColumn Header="مضمون" Binding="{Binding SubjectName}" Width="120" />
                                    <DataGridTextColumn Header="صنف/درجه" Binding="{Binding GradeLevel}" Width="100" />
                                    <DataGridTextColumn Header="تعداد کل" Binding="{Binding TotalCopies}" Width="80" />
                                    <DataGridTextColumn Header="موجود" Binding="{Binding AvailableCopies}" Width="80" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Tab 2: Issue & Return -->
            <TabItem Header="🎒 توزیع و بازگشت">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- Issue Form -->
                    <Border Grid.Row="0" Background="#FFF7ED" BorderBrush="#FED7AA" BorderThickness="1" Padding="10" CornerRadius="6" Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="220" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="130" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="180" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="کتاب درسی:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,6,0" />
                            <ComboBox x:Name="IssueTextbookComboBox" Grid.Column="1" Height="32" Margin="0,0,10,0" DisplayMemberPath="Title" SelectedValuePath="TextbookId" />

                            <TextBlock Grid.Column="2" Text="صنف:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,6,0" />
                            <ComboBox x:Name="IssueClassComboBox" Grid.Column="3" Height="32" Margin="0,0,10,0" DisplayMemberPath="GradeName" SelectedValuePath="ClassId" SelectionChanged="IssueClassComboBox_SelectionChanged" />

                            <TextBlock Grid.Column="4" Text="شاگرد:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,6,0" />
                            <ComboBox x:Name="IssueStudentComboBox" Grid.Column="5" Height="32" Margin="0,0,10,0" DisplayMemberPath="DisplayName" SelectedValuePath="StudentId" />

                            <Button Grid.Column="6" Content="🎒 توزیع کتاب" Height="32" Padding="14,0" Background="#EA580C" Foreground="White" FontWeight="Bold" Click="IssueTextbookButton_Click" />
                        </Grid>
                    </Border>

                    <!-- Issues DataGrid -->
                    <Border Grid.Row="1" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="8">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <WrapPanel Margin="4,4,4,8">
                                <TextBlock Text="لیست کتب توزیع‌شده" FontWeight="Bold" FontSize="15" Foreground="#334155" VerticalAlignment="Center" />
                                <CheckBox x:Name="ActiveIssuesOnlyCheckBox" Content="فقط بازنیاورده‌ها" Margin="14,0,0,0" IsChecked="True" VerticalAlignment="Center" Checked="IssueFilter_Changed" Unchecked="IssueFilter_Changed" />
                                <Button Content="🔄 تازه‌سازی" Height="28" Padding="10,0" Margin="14,0,0,0" Background="#3B82F6" Foreground="White" Click="RefreshIssuesButton_Click" />
                            </WrapPanel>
                            <DataGrid x:Name="IssuesDataGrid"
                                      Grid.Row="1"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      IsReadOnly="True"
                                      SelectionMode="Single"
                                      GridLinesVisibility="Horizontal"
                                      HorizontalGridLinesBrush="#E2E8F0"
                                      RowHeight="32">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="آیدی" Binding="{Binding IssueId}" Width="55" />
                                    <DataGridTextColumn Header="کتاب" Binding="{Binding TextbookTitle}" Width="*" />
                                    <DataGridTextColumn Header="شاگرد" Binding="{Binding StudentName}" Width="*" />
                                    <DataGridTextColumn Header="شماره اساس" Binding="{Binding RollNumber}" Width="85" />
                                    <DataGridTextColumn Header="تاریخ توزیع" Binding="{Binding IssueDate}" Width="100" />
                                    <DataGridTextColumn Header="بازگشت" Binding="{Binding ReturnDateText}" Width="100" />
                                    <DataGridTextColumn Header="وضعیت" Binding="{Binding StatusText}" Width="120" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="4,8,4,0">
                        <Button Content="📥 ثبت بازگشت کتاب انتخاب‌شده" Height="34" Padding="14,0" Background="#16A34A" Foreground="White" FontWeight="Bold" Click="ReturnTextbookButton_Click" />
                        <TextBlock x:Name="IssueStatusTextBlock" Text="" FontSize="12" Foreground="#059669" FontWeight="SemiBold" VerticalAlignment="Center" Margin="12,0,0,0" />
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
