<UserControl x:Class="Maktab.App.Wpf.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             FlowDirection="RightToLeft">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#334155" Padding="14" CornerRadius="6" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="📊 گزارش‌ها و خروجی اکسل" FontSize="20" FontWeight="Bold" Foreground="White" />
                <TextBlock Text=" (Reports &amp; Excel Export)" FontSize="13" Foreground="#CBD5E1" VerticalAlignment="Center" Margin="10,2,0,0" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="14" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#FAFAFA" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="14">
                <StackPanel>
                    <TextBlock Text="تنظیمات گزارش" FontSize="16" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,12" />

                    <TextBlock Text="صنف *" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <ComboBox x:Name="ClassComboBox" Height="32" Margin="0,0,0,10" DisplayMemberPath="GradeName" SelectedValuePath="ClassId" />

                    <TextBlock Text="سال تعلیمی *" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <ComboBox x:Name="AcademicYearComboBox" Height="32" Margin="0,0,0,10" DisplayMemberPath="YearName" SelectedValuePath="AcademicYearId" />

                    <TextBlock Text="مضمون (برای خروجی نمرات)" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <ComboBox x:Name="SubjectComboBox" Height="32" Margin="0,0,0,16" DisplayMemberPath="SubjectName" SelectedValuePath="SubjectId" />

                    <Button Height="36" Content="📈 عملکرد صنف (Excel)" Background="#2563EB" Foreground="White" FontWeight="Bold" Margin="0,0,0,8" Click="ExportClassPerformanceButton_Click" />
                    <Button Height="36" Content="📊 توزیع نمرات (Excel)" Background="#2563EB" Foreground="White" FontWeight="Bold" Margin="0,0,0,8" Click="ExportGradeDistributionButton_Click" />
                    <Button Height="36" Content="👨‍🎓 خروجی شاگردان" Background="#16A34A" Foreground="White" FontWeight="Bold" Margin="0,0,0,8" Click="ExportStudentsButton_Click" />
                    <Button Height="36" Content="📝 خروجی نمرات" Background="#16A34A" Foreground="White" FontWeight="Bold" Margin="0,0,0,8" Click="ExportMarksButton_Click" />
                    <Button Height="36" Content="🗓️ خروجی حاضری" Background="#16A34A" Foreground="White" FontWeight="Bold" Margin="0,0,0,8" Click="ExportAttendanceButton_Click" />
                    <Button Height="36" Content="💰 خروجی فیس‌ها" Background="#16A34A" Foreground="White" FontWeight="Bold" Click="ExportFeesButton_Click" />
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="پیش‌نمایش" FontWeight="Bold" FontSize="15" Foreground="#334155" Margin="4,4,4,8" />
                    <DataGrid x:Name="PreviewDataGrid"
                              Grid.Row="1"
                              AutoGenerateColumns="True"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="#E2E8F0"
                              RowHeight="32" />
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
