<UserControl x:Class="Maktab.App.Wpf.Views.AcademicYearView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             FlowDirection="RightToLeft">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#334155" Padding="14" CornerRadius="6" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="📅 سال‌های تعلیمی" FontSize="20" FontWeight="Bold" Foreground="White" />
                <TextBlock Text=" (Academic Years)" FontSize="13" Foreground="#CBD5E1" VerticalAlignment="Center" Margin="10,2,0,0" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="14" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#FAFAFA" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="14">
                <StackPanel>
                    <TextBlock Text="سال تعلیمی جدید" FontSize="16" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,12" />

                    <TextBlock Text="نام سال (مثلاً ۱۴۰۵ - ۱۴۰۶) *" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <TextBox x:Name="YearNameTextBox" Height="32" Margin="0,0,0,10" MaxLength="20" />

                    <TextBlock Text="تاریخ شروع *" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <DatePicker x:Name="StartDatePicker" Height="32" Margin="0,0,0,10" />

                    <TextBlock Text="تاریخ پایان *" FontWeight="SemiBold" Margin="0,0,0,4" />
                    <DatePicker x:Name="EndDatePicker" Height="32" Margin="0,0,0,16" />

                    <Button Height="36" Content="➕ افزودن سال" Background="#16A34A" Foreground="White" FontWeight="Bold" Click="AddYearButton_Click" />
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="سال‌های تعلیمی" FontWeight="Bold" FontSize="15" Foreground="#334155" />
                        <Button Grid.Column="1" Content="🔄 تازه‌سازی" Height="28" Padding="10,0" Background="#3B82F6" Foreground="White" FontWeight="SemiBold" Click="RefreshButton_Click" />
                    </Grid>

                    <DataGrid x:Name="YearsDataGrid"
                              Grid.Row="1"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="#E2E8F0"
                              RowHeight="32">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding AcademicYearId}" Width="50" />
                            <DataGridTextColumn Header="نام سال" Binding="{Binding YearName}" Width="*" />
                            <DataGridTextColumn Header="شروع" Binding="{Binding StartDate, StringFormat={}{0:yyyy/MM/dd}}" Width="100" />
                            <DataGridTextColumn Header="پایان" Binding="{Binding EndDate, StringFormat={}{0:yyyy/MM/dd}}" Width="100" />
                            <DataGridTextColumn Header="فعال" Binding="{Binding IsActive}" Width="70" />
                            <DataGridTemplateColumn Header="عملیات" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="فعال‌سازی" Background="#2563EB" Foreground="White" FontSize="12" Padding="8,4"
                                                Click="SetActiveButton_Click" Tag="{Binding AcademicYearId}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
