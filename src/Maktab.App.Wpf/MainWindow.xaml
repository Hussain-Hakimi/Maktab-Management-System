<Window x:Class="Maktab.App.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="سیستم مدیریت مکتب - Afghan School Management System"
        Height="760"
        Width="1200"
        MinHeight="600"
        MinWidth="900"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Language="fa-IR"
        FlowDirection="RightToLeft">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Top Header Bar -->
        <Border Grid.Row="0" Background="#0F172A" Padding="16,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📘" FontSize="24" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="سیستم مدیریت مکتب" FontSize="20" FontWeight="Bold" Foreground="White" />
                        <TextBlock Text="Afghan School Management System — V1.0.1" FontSize="11" Foreground="#94A3B8" Margin="0,2,0,0" FlowDirection="LeftToRight" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="SchoolYearTextBlock" Text="" FontSize="12" Foreground="#CBD5E1" VerticalAlignment="Center" Margin="0,0,12,0" />
                    <TextBlock x:Name="CurrentDateTextBlock" Text="" FontSize="12" Foreground="#CBD5E1" VerticalAlignment="Center" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Tab Navigation -->
        <TabControl x:Name="MainTabControl"
                    Grid.Row="1"
                    Padding="0"
                    BorderThickness="0"
                    Background="#F1F5F9">

            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Padding" Value="18,10" />
                    <Setter Property="FontSize" Value="14" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                    <Setter Property="Foreground" Value="#475569" />
                    <Setter Property="Background" Value="#E2E8F0" />
                    <Setter Property="Margin" Value="0,0,2,0" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="TabBorder"
                                        Background="{TemplateBinding Background}"
                                        Padding="{TemplateBinding Padding}"
                                        CornerRadius="6,6,0,0"
                                        Margin="0,4,2,0"
                                        BorderThickness="1,1,1,0"
                                        BorderBrush="#CBD5E1"
                                        Cursor="Hand">
                                    <ContentPresenter ContentSource="Header"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="TabBorder" Property="Background" Value="White" />
                                        <Setter Property="Foreground" Value="#0F172A" />
                                        <Setter TargetName="TabBorder" Property="BorderBrush" Value="#3B82F6" />
                                        <Setter TargetName="TabBorder" Property="BorderThickness" Value="1,2,1,0" />
                                    </Trigger>
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="IsSelected" Value="False" />
                                            <Condition Property="IsMouseOver" Value="True" />
                                        </MultiTrigger.Conditions>
                                        <Setter TargetName="TabBorder" Property="Background" Value="#CBD5E1" />
                                    </MultiTrigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <!-- Tab 1: Classes & Subjects -->
            <TabItem Header="🏫  صنف‌ها و مضامین">
                <ContentControl x:Name="ClassSubjectContent" />
            </TabItem>

            <!-- Tab 2: Students -->
            <TabItem Header="👨‍🎓  شاگردان">
                <ContentControl x:Name="StudentManagementContent" />
            </TabItem>

            <!-- Tab 3: Marks Entry -->
            <TabItem Header="📝  ثبت نمرات">
                <ContentControl x:Name="MarksEntryContent" />
            </TabItem>

            <!-- Tab 4: Attendance -->
            <TabItem Header="🗓️  حاضری روزانه">
                <ContentControl x:Name="AttendanceContent" />
            </TabItem>
            <TabItem Header="📚  کتابخانه">
                 <ContentControl x:Name="LibraryContent" />
            </TabItem>
           <TabItem Header="📦  کتاب‌های درسی">
                 <ContentControl x:Name="TextbookContent" />
            </TabItem>
           <TabItem Header="💰  فیس‌ها">
                 <ContentControl x:Name="FeesContent" />
           </TabItem>

            <!-- Tab 5: Report Cards -->
            <TabItem Header="📄  کارنامه / اطلاع‌نامه">
                <ContentControl x:Name="ReportCardsContent" />
            </TabItem>

            <!-- Tab 6: Backup & Settings -->
            <TabItem Header="💾  پشتیبان‌گیری و تنظیمات">
                <ContentControl x:Name="BackupSettingsContent" />
            </TabItem>
        </TabControl>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#0F172A" Padding="12,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusBarText" Text="آماده" FontSize="12" Foreground="#94A3B8" VerticalAlignment="Center" />
                <TextBlock Grid.Column="1" Text=" توسعه دهنده حسین حکیمی❤️ برای مکاتب افغانستان" FontSize="11" Foreground="#64748B" VerticalAlignment="Center" />
            </Grid>
        </Border>
    </Grid>
</Window>
